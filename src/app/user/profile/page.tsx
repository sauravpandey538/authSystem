import React from 'react';

interface ComponentNameProps {

}

const Profile: React.FC<ComponentNameProps> = ({ }) => {
    return (
        <div>
            This is profile page when the auth was sucessfully done
        </div>
    );
};

export default Profile;